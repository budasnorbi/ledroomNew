/* eslint-disable import/prefer-default-export */
import { addSelection, selectSelection, deleteSelection } from '../../actions/actions';

export const mapDispatchToProps = {
  addSelection,
  selectSelection,
  deleteSelection,
};
