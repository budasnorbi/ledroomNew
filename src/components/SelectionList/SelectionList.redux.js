/* eslint-disable import/prefer-default-export */
import {
  addSelection, selectSelection, deleteSelection, closeColorPicker,
} from '../../actions/actions';

export const mapDispatchToProps = {
  addSelection,
  selectSelection,
  deleteSelection,
  closeColorPicker,
};
