import { moveColorPicker, selectColor, selectSelection, openColorPicker } from '../../actions/actions';

export const mapDispatchToProps = {
  moveColorPicker,
  selectColor,
  selectSelection,
  openColorPicker,
};
